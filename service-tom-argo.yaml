apiVersion: v1
kind: Service
metadata:
  labels:
    app: tomcat
  name: tomcat
  namespace: default
spec:
  ports:
  - port: 30544
    protocol: TCP
    targetPort: 8089
  selector:
    app: tomcat
  type: NodePort
